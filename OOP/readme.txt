
objects, class, attributes, methods, inheritances, overriding methods.

callable object. example: object()

all user defined functions created using def or lambda() expressions are callable.

4. class attributes are shared by all instances of the class.

6. In Python, we use static methods to group logically related functions in a class.
To define a static method, we use the @staticmethod decorator.


